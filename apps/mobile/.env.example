# Copy this to .env and update with YOUR Mac's LAN IP (ipconfig getifaddr en0).
# iPhone on WiFi cannot reach 'localhost' — it maps to the phone itself, not the Mac.
# For iOS Simulator you CAN use localhost.
EXPO_PUBLIC_API_BASE_URL=http://192.168.1.1:3000/api/v1
